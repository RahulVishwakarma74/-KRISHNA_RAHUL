int sum(int a, int b) {
    return a + b;   // 1. Function adds a and b
}

int main() {
    int result = sum(5, 3);  // 2. Function called with a=5, b=3
    printf("%d", result);     // 3. Prints the returned value
    return 0;
}